# Test script to verify the installed version of the LangChain framework
import langchain  # Import the main langchain package

# Print the installed version string of langchain to console
print(langchain.__version__)
